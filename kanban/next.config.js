/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ["cdn-icons-png.flaticon.com"] , //เพิ่ม image domain ที่เราเลือกใช้
    },
};

module.exports = nextConfig
